<div class="banner d-flex">
  <%= form_tag root_path, method: :get do %>
    <%= text_field_tag :query,
      params[:query],
      class: "form-control",
      placeholder: "Search by name or city"
    %>
    <%= submit_tag "Search", class: "btn btn-info" %>
  <% end %>
<!-- <input class="form-control d-inline-flex justify-content-aroundd-flex " type="text" placeholder="What baby supply are you looking for?" aria-label="Search">
<input class="form-control d-inline-flex justify-content-aroundd-flex " type="text" placeholder="Enter a location" aria-label="Search">
<div class="search-button d-inline-flex"> -->
<!-- <i class="fas fa-search"></i> -->
<!-- </div> -->
</div>

<ul>
  <div class="card-container">
    <% @products.each do |product| %>
      <div class="card">
        <i class="far fa-heart fav-prod" style="color: #DE7362;"></i>
        <div class="imgBox">
          <%= image_tag "crib.jpg" %>
          <%# <img src="../app/assets/images/crib.jpg"> %>
        </div>
        <div class="details">
          <div class="textContent">
            <h3><%= product.name %></h3>
            <h2 class="price"><%= product.price %> €/day</h2>
          </div>
          <p><%= link_to 'See more', product_path(product), class: 'btn btn-primary' %></p>
        </div>
      </div>
    <% end %>
  </div>
</ul>

