<div class="container">
  <h1><%= @product.name %></h1>
  <p><%= @product.description %></p>
  <p><%= @product.price %> €</p>
  <%= link_to 'Book', new_product_booking_path(@product), class: 'btn btn-primary' %>
</div>

